 #include<iostream>
using namespace std;
int main(){
    int num,den;
    cout<<"enter two num"<<endl;
    cin>>num>>den;
    try{
        if (den==0)
        throw(den);
    else{
    cout<<num/dec;
    }
}
    catch(int den)
    {
        cout<<"division by zero error:cannot divide by"<<d;
    }
}