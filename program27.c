#include<stdio.h>
int main()
{
    int a[10],i,size=0 ,sum=0;
    scanf("%d",&size);
    {
        for( int i=0;i<size; scanf("%d" ,&a[i++])); 
    }
    {
      for(i=0;i<=size;("%d",a[i]),sum+=a[i++]);  
    } 
    printf("sum of all elements is %d",sum);
    return 0;
}
